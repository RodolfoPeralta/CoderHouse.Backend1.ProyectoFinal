<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Api Rest - Products</title>
</head>
<body>
    <h2>Lista de productos</h2>
    {{#if products.length}}
        <ul>
            {{#each products}}
                <li>{{title}}</li>
            {{/each}}
        </ul>
    {{else}}
        <p>Lista de productos vacía</p>
    {{/if}}
</body>
</html>